[package]
name = "booklet"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"

[dependencies]
qtbridge = { version = "0.2", features = ["serde_json"] }
pulldown-cmark = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"
regex = "1"
